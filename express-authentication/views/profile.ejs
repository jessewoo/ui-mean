<!DOCTYPE html>
<html>
	<% include partials/head.ejs %>

	<body>
		<% include partials/navbar.ejs %>
		<div class="container">
			<div class="row">
				<div class="col-sm-12">
					<br/><br/><br/><br/>
					<div>
						<h3>User Profile</h3>
						<span class="fa fa-sign-out pull-right"><a href="/logout"> Sign out</a></span>
						<span><strong>user id</strong>: <%= user._id ? user._id : '' %></span>
						<br><br>
						<hr>
						<% if (user.local.email != null) { %>
							<p>Local User Email</p><br>
							<strong>id</strong>: <%= user._id %><br>
							<strong>email</strong>: <%= user.local.email %><br>
						<% } %>
						<% if (user.google.id != null) { %>
							<p class="fa fa-google-plus"> Google</p><br>
							<strong>id</strong>: <%= user.google.id %><br>
							<strong>email</strong>: <%= user.google.email %><br>
							<strong>name</strong>: <%= user.google.name %>
						<% } %>
					</div>
				</div>
			</div>
		</div>
	</body>
</html>